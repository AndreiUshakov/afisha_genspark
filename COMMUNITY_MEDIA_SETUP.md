# Настройка медиагалереи сообщества

## Обзор

Реализована система загрузки и управления изображениями для галереи сообществ на странице `/dashboard/community/[slug]/media`. Владельцы сообществ могут загружать и удалять изображения с использованием бакета `community-media`.

## Реализованные функции

### ✅ Групповая загрузка изображений
- Drag & drop интерфейс для удобной загрузки
- Поддержка множественной загрузки (до 10 файлов за раз)
- Предпросмотр выбранных файлов перед загрузкой
- Валидация форматов (JPG, PNG, WebP)
- Валидация размера файла (макс. 10MB)
- Лимит на общее количество изображений (200 на сообщество)

### ✅ Управление галереей
- Адаптивная сетка с превью изображений
- Lightbox для полноэкранного просмотра
- Удаление изображений с подтверждением
- Статистика: количество фото, доступные слоты, дата последней загрузки
- Переключение между режимами просмотра и загрузки

### ✅ Безопасность
- Row Level Security (RLS) политики
- Доступ только для владельцев сообщества
- Валидация на стороне сервера
- Автоматическая очистка при удалении

## Структура файлов

```
app/dashboard/community/[slug]/media/
├── page.tsx                    # Server component - загрузка данных
├── CommunityMediaClient.tsx    # Client component - UI и интерактивность
└── actions.ts                  # Server actions для загрузки/удаления

supabase/
├── migrations/
│   └── create_community_media_table.sql   # Миграция БД
└── storage/
    └── create_community_media_bucket.sql  # Настройка бакета
```

## Установка

### 1. Создание таблицы в БД

Выполните SQL из файла [`supabase/migrations/create_community_media_table.sql`](supabase/migrations/create_community_media_table.sql:1) в Supabase SQL Editor:

```sql
-- Создает таблицу community_media с полями:
-- - id, community_id, file_path, file_url
-- - file_name, file_size, mime_type
-- - uploaded_at, created_at, updated_at
-- + индексы и RLS политики
```

### 2. Создание Storage Bucket

Выполните SQL из файла [`supabase/storage/create_community_media_bucket.sql`](supabase/storage/create_community_media_bucket.sql:1):

```sql
-- Создает бакет 'community-media' с:
-- - Публичным доступом для чтения
-- - Лимитом 10MB на файл
-- - Поддержкой JPEG, PNG, WebP
-- + RLS политики для upload/view/delete
```

**Альтернативно**, создайте бакет вручную в Supabase Dashboard:
1. Перейдите в Storage → Create bucket
2. Название: `community-media`
3. Public bucket: ✅ (включено)
4. File size limit: `10485760` (10MB)
5. Allowed MIME types: `image/jpeg, image/png, image/webp`

### 3. Настройка RLS политик для Storage

Если создавали бакет вручную, добавьте политики из SQL файла или через Dashboard:

**Политики для storage.objects:**
- `Community owners can upload media` - владельцы могут загружать
- `Community owners can view their media` - владельцы видят свои файлы
- `Community owners can delete their media` - владельцы могут удалять
- `Public can view published community media` - публичный просмотр опубликованных

## Использование

### Доступ к галерее

Владелец сообщества может перейти на страницу медиагалереи:
```
/dashboard/community/[slug]/media
```

где `[slug]` - это slug вашего сообщества.

### Загрузка изображений

1. Нажмите кнопку "⬆️ Загрузить"
2. Перетащите файлы в область загрузки или нажмите для выбора
3. Проверьте превью выбранных файлов
4. Нажмите "Загрузить N файлов"

### Просмотр и удаление

1. В режиме "📚 Галерея" видна сетка всех изображений
2. При наведении появляются кнопки просмотра и удаления
3. Клик на изображение открывает полноэкранный просмотр
4. Кнопка удаления запрашивает подтверждение

## API Actions

### [`uploadCommunityMedia(formData)`](app/dashboard/community/[slug]/media/actions.ts:6)

Загружает изображения в галерею сообщества.

**Параметры:**
- `communityId` - ID сообщества
- `files` - массив File объектов

**Возвращает:**
```typescript
{
  success: boolean
  uploaded?: number      // количество загруженных файлов
  errors?: string[]      // ошибки для отдельных файлов
  error?: string         // общая ошибка
}
```

### [`deleteCommunityMedia(mediaId, communitySlug)`](app/dashboard/community/[slug]/media/actions.ts:127)

Удаляет изображение из галереи.

**Параметры:**
- `mediaId` - ID медиа записи
- `communitySlug` - slug сообщества

**Возвращает:**
```typescript
{
  success: boolean
  error?: string
}
```

### [`getCommunityMedia(communitySlug)`](app/dashboard/community/[slug]/media/actions.ts:178)

Получает все медиа сообщества (используется в server component).

## Схема БД

### Таблица `community_media`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Первичный ключ |
| `community_id` | UUID | FK на communities |
| `file_path` | TEXT | Путь в storage (community_id/filename) |
| `file_url` | TEXT | Прокси URL (/api/storage/...) |
| `file_name` | TEXT | Оригинальное имя файла |
| `file_size` | INTEGER | Размер в байтах |
| `mime_type` | TEXT | MIME тип (image/jpeg, etc) |
| `uploaded_at` | TIMESTAMPTZ | Дата загрузки |

### Storage структура

```
community-media/
└── {community_id}/
    ├── {timestamp}-{random}.jpg
    ├── {timestamp}-{random}.png
    └── {timestamp}-{random}.webp
```

## Ограничения

- **Максимум файлов**: 200 изображений на сообщество
- **Размер файла**: до 10MB
- **Форматы**: JPEG, PNG, WebP
- **Одновременная загрузка**: до 10 файлов за раз

## Примеры ошибок и решения

### "Сообщество не найдено"
- Проверьте, что вы владелец сообщества
- Убедитесь, что slug корректный

### "Превышен лимит"
- Удалите старые изображения
- Лимит: 200 изображений на сообщество

### "Ошибка при загрузке"
- Проверьте формат файла (JPG/PNG/WebP)
- Проверьте размер файла (макс. 10MB)
- Убедитесь, что bucket создан и политики настроены

### Storage Policy Error
- Выполните SQL из `create_community_media_bucket.sql`
- Проверьте RLS политики в Supabase Dashboard

## Технические детали

### Proxy URL для изображений

Используется прокси `/api/storage/community-media/{path}` вместо прямых Supabase URL для:
- Избежания mixed content ошибок
- Единого домена для всех ресурсов
- Возможности добавления middleware

### Очистка при удалении

При удалении изображения:
1. Удаляется файл из Storage bucket
2. Удаляется запись из таблицы `community_media`
3. Cascade delete при удалении сообщества

### Оптимизация

- Индексы на `community_id` и `uploaded_at`
- Lazy loading изображений в grid
- Автоматическая ревалидация кеша после операций

## Дальнейшие улучшения

Возможные расширения функционала:

- [ ] Редактирование метаданных (название, описание)
- [ ] Массовое удаление изображений
- [ ] Сортировка и фильтрация
- [ ] Использование изображений в контенте сообщества
- [ ] Оптимизация и ресайз изображений
- [ ] CDN для быстрой загрузки
- [ ] Альбомы/категории для организации

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Проверьте логи Supabase
3. Убедитесь, что все миграции выполнены
4. Проверьте RLS политики
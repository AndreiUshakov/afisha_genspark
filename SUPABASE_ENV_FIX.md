# 🔧 Исправление ошибки ENOTFOUND supabase.sober-automation.ru

## Проблема

В логах Amvera вы видите ошибку:
```
Error: getaddrinfo ENOTFOUND supabase.sober-automation.ru
TypeError: fetch failed
AuthRetryableFetchError: fetch failed
```

## Причина

**Переменные окружения Supabase не настроены на хостинге Amvera.**

Приложение не может подключиться к базе данных, потому что не знает адрес Supabase.

## Быстрое решение

### Шаг 1: Получите данные Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (длинная строка)

### Шаг 2: Добавьте переменные в Amvera

1. Откройте [Amvera Console](https://console.amvera.io)
2. Выберите проект `afisha_genspark`
3. Откройте **Settings** (⚙️)
4. Найдите **Environment Variables**
5. Добавьте две переменные:

   **Переменная 1:**
   ```
   Имя: NEXT_PUBLIC_SUPABASE_URL
   Значение: ваш_project_url_из_supabase
   ```

   **Переменная 2:**
   ```
   Имя: NEXT_PUBLIC_SUPABASE_ANON_KEY
   Значение: ваш_anon_key_из_supabase
   ```

### Шаг 3: Перезапустите приложение

1. Нажмите **Save** в Amvera
2. Нажмите **Redeploy** (полная пересборка)
3. Дождитесь завершения (3-5 минут)

### Шаг 4: Проверка

1. Откройте ваш сайт
2. Попробуйте войти/зарегистрироваться
3. Проверьте логи - ошибка должна исчезнуть

## Важные моменты

✅ **Обязательно делайте Redeploy**, а не просто Restart
✅ Проверьте правильность написания имен переменных (регистр важен!)
✅ Убедитесь, что скопировали полные значения (без пробелов в начале/конце)

❌ НЕ коммитьте `.env.local` в Git
❌ НЕ используйте Build Variables (только Environment Variables)

## Проверка переменных

После добавления переменных, они должны быть видны в логах сборки Amvera как:
```
NEXT_PUBLIC_SUPABASE_URL: https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbG...
```

Если их нет - вы добавили их в неправильное место.

## Дополнительная помощь

Полная инструкция: [`AMVERA_ENV_SETUP.md`](./AMVERA_ENV_SETUP.md)

## Тест локально

Чтобы избежать таких проблем в будущем, всегда тестируйте локально:

```bash
# Создайте .env.local
cp .env.example .env.local

# Заполните значениями из Supabase

# Запустите локально
npm run dev
```

---

**💡 После исправления ошибка "ENOTFOUND" исчезнет и приложение сможет подключаться к Supabase!**
# Настройка переменных окружения в Amvera

## 🚨 КРИТИЧЕСКИ ВАЖНО

Если вы видите ошибку в логах Amvera:
```
Error: getaddrinfo ENOTFOUND supabase.sober-automation.ru
AuthRetryableFetchError: fetch failed
```

**Это означает, что переменные окружения Supabase НЕ настроены!**

## 📋 Необходимые переменные

Для работы приложения требуются следующие переменные окружения:

1. `NEXT_PUBLIC_SUPABASE_URL` - URL вашего Supabase проекта
2. `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Публичный ключ Supabase
3. `SUPABASE_SERVICE_ROLE_KEY` - Серверный ключ Supabase (опционально)

## 🔑 Где взять значения

1. Откройте ваш проект в [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдите в **Settings** → **API**
3. Скопируйте:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon/public key** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role key** → `SUPABASE_SERVICE_ROLE_KEY`

## ⚙️ Как добавить в Amvera

### Шаг 1: Откройте настройки проекта

1. Войдите в [панель управления Amvera](https://console.amvera.io)
2. Выберите ваш проект `afisha_genspark`
3. Перейдите в **Settings** (⚙️)

### Шаг 2: Добавьте переменные окружения

1. Найдите раздел **Environment Variables**
2. Нажмите **Add Variable** или **+ Add**
3. Добавьте каждую переменную:

```
Имя: NEXT_PUBLIC_SUPABASE_URL
Значение: https://ваш-проект.supabase.co
```

```
Имя: NEXT_PUBLIC_SUPABASE_ANON_KEY
Значение: ваш_публичный_ключ
```

```
Имя: SUPABASE_SERVICE_ROLE_KEY
Значение: ваш_серверный_ключ
```

### Шаг 3: Сохраните и перезапустите

1. Нажмите **Save** или **Apply**
2. Сделайте **Redeploy** проекта
3. Дождитесь завершения деплоя (3-5 минут)

## ✅ Проверка

После настройки переменных и redeploy:

1. Откройте ваш сайт на Amvera
2. Попробуйте войти или зарегистрироваться
3. Проверьте логи - ошибок `ENOTFOUND` быть не должно

## 🐛 Решение проблем

### Проблема: Переменные не применяются

**Решение:**
1. Убедитесь, что вы сделали **Redeploy** после добавления переменных
2. Проверьте правильность написания имен переменных (регистр важен!)
3. Убедитесь, что переменные `NEXT_PUBLIC_*` доступны в браузере

### Проблема: Ошибка "Missing Supabase environment variables"

**Решение:**
1. Проверьте логи сборки в Amvera
2. Убедитесь, что переменные добавлены именно в **Environment Variables**, а не в Build Variables
3. Пересоберите проект: Settings → Redeploy

### Проблема: Неправильные значения

**Решение:**
1. Перейдите в Supabase Dashboard → Settings → API
2. Скопируйте значения заново
3. Обновите переменные в Amvera
4. Redeploy

## 📝 Локальная разработка

Для локальной разработки создайте файл `.env.local`:

```bash
# Скопируйте .env.example
cp .env.example .env.local

# Заполните значениями из Supabase Dashboard
```

**ВАЖНО:** Файл `.env.local` НЕ должен коммититься в Git (уже добавлен в `.gitignore`)

## 🔒 Безопасность

- ✅ `NEXT_PUBLIC_*` - безопасно использовать в браузере
- ❌ `SUPABASE_SERVICE_ROLE_KEY` - НИКОГДА не публикуйте и не используйте в клиентском коде
- ❌ Не коммитьте `.env.local` или `.env` в Git
- ✅ Используйте `.env.example` только как шаблон

## 📚 Дополнительные ресурсы

- [Документация Amvera - Environment Variables](https://docs.amvera.io/deployment/env-vars)
- [Next.js - Environment Variables](https://nextjs.org/docs/app/building-your-application/configuring/environment-variables)
- [Supabase - API Settings](https://supabase.com/docs/guides/api)

---

**💡 Совет:** После добавления переменных окружения всегда делайте полный Redeploy, а не просто Restart!
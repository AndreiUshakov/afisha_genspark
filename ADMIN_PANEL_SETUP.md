# Настройка админ-панели

Это руководство описывает настройку и использование админ-панели для модерации контента на сайте "Афиша Иркутска".

## Содержание

1. [Обзор функционала](#обзор-функционала)
2. [Установка и миграция БД](#установка-и-миграция-бд)
3. [Создание администратора](#создание-администратора)
4. [Доступ к админ-панели](#доступ-к-админ-панели)
5. [Функции модерации](#функции-модерации)

## Обзор функционала

Админ-панель предоставляет следующие возможности:

### 🔍 Предмодерация
- **Сообщества**: Новые сообщества проходят обязательную предмодерацию перед публикацией
- Автоматическое создание задач модерации при создании сообщества

### 📝 Постмодерация
- **Мероприятия**: Просмотр и управление всеми событиями
- **Посты блога**: Модерация публикаций в блоге
- **Эксперты**: Управление профилями экспертов

### ⚙️ Управление
- Публикация/снятие с публикации контента
- Активация/деактивация профилей экспертов
- Просмотр статистики модерации
- Список задач модерации с приоритетами

## Установка и миграция БД

### Шаг 1: Применение миграции

Выполните SQL-миграцию для создания необходимых таблиц и политик:

```bash
# Если используете Supabase CLI
supabase db push

# Или выполните миграцию напрямую в Supabase Dashboard
# SQL Editor -> New query -> Вставьте содержимое файла:
# supabase/migrations/20241211_create_admin_moderation.sql
```

Миграция создаст:
- Таблицу `moderation_tasks` для задач модерации
- RLS политики для админов
- Триггеры для автоматического создания задач
- Вспомогательные функции

### Шаг 2: Проверка миграции

Убедитесь, что таблица создана:

```sql
SELECT * FROM moderation_tasks LIMIT 1;
```

## Создание администратора

### Способ 1: Через Supabase Dashboard

1. Откройте Supabase Dashboard
2. Перейдите в **Table Editor** → **profiles**
3. Найдите нужного пользователя по email
4. Измените поле `role` на `admin`

### Способ 2: Через SQL

```sql
-- Обновить роль существующего пользователя
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'admin@example.com';

-- Проверить
SELECT email, role FROM profiles WHERE role = 'admin';
```

### Способ 3: При регистрации нового пользователя

1. Зарегистрируйте нового пользователя через `/auth/register`
2. Сразу после регистрации выполните SQL:

```sql
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'новый_admin@example.com';
```
## Важные замечания по импортам

В проекте используются абсолютные импорты через алиас `@/`. Все импорты server actions в client components должны использовать абсолютные пути:

```typescript
// ✅ Правильно
import { toggleCommunityPublishStatus } from '@/app/admin/communities/actions';

// ❌ Неправильно
import { toggleCommunityPublishStatus } from '../actions';
```

Это относится ко всем таблицам и компонентам в админ-панели.


## Доступ к админ-панели

### URL админ-панели

```
https://ваш-домен.ru/admin
```

### Защита доступа

Админ-панель автоматически проверяет роль пользователя:

1. Только пользователи с `role = 'admin'` имеют доступ
2. При попытке доступа без прав происходит редирект на `/dashboard`
3. Все запросы защищены RLS политиками

### Навигация

После входа доступны разделы:

- **Главная** (`/admin`) - Дашборд со статистикой
- **Модерация** (`/admin/moderation`) - Задачи модерации
- **Сообщества** (`/admin/communities`) - Управление сообществами
- **Мероприятия** (`/admin/events`) - Управление событиями
- **Посты блога** (`/admin/posts`) - Управление постами
- **Эксперты** (`/admin/experts`) - Управление экспертами

## Функции модерации

### Задачи модерации

**Автоматическое создание:**
- При создании сообщества автоматически создается задача модерации
- Задача имеет статус `pending` и приоритет `normal`

**Статусы задач:**
- `pending` - Ожидает проверки
- `in_review` - На рассмотрении
- `approved` - Одобрено
- `rejected` - Отклонено

**Приоритеты:**
- `low` - Низкий
- `normal` - Обычный
- `high` - Высокий
- `urgent` - Срочный

### Управление сообществами

**Предмодерация:**
1. Новое сообщество создается с `is_published = false`
2. Создается задача модерации
3. Администратор просматривает сообщество
4. Публикует или отклоняет

**Действия:**
- **Опубликовать** - Делает сообщество видимым всем пользователям
- **Снять с публикации** - Скрывает сообщество от пользователей

### Управление мероприятиями

**Постмодерация:**
- События могут быть опубликованы создателем
- Админ может снять событие с публикации при необходимости

**Фильтры:**
- Все события
- Опубликованные
- Не опубликованные

### Управление постами

**Постмодерация:**
- Посты могут быть от сообществ или экспертов
- Админ управляет видимостью постов

**Отображение:**
- Название и описание поста
- Автор (email и имя)
- Источник (сообщество или эксперт)
- Дата создания

### Управление экспертами

**Модерация профилей:**
- Активация/деактивация экспертов
- Просмотр рейтинга и отзывов

**Статусы:**
- `is_active = true` - Эксперт активен и виден всем
- `is_active = false` - Эксперт деактивирован

## RLS Политики

### Для администраторов

Администраторы имеют расширенные права:

```sql
-- Видят все сообщества (включая неопубликованные)
SELECT * FROM communities; -- Работает для админов

-- Могут обновлять любые сообщества
UPDATE communities SET is_published = true WHERE id = '...';

-- Аналогично для events, posts, experts
```

### Для обычных пользователей

Обычные пользователи видят только:
- Опубликованные сообщества (`is_published = true`)
- Опубликованные события (`is_published = true`)
- Активных экспертов (`is_active = true`)
- Свой собственный контент (независимо от статуса)

## Безопасность

### Проверки на уровне БД

1. **RLS политики** - Автоматическая фильтрация данных
2. **Проверка роли** - Функции проверяют `profiles.role = 'admin'`
3. **Триггеры** - Автоматическое управление задачами

### Проверки на уровне приложения

1. **Middleware** - Проверка прав доступа к `/admin`
2. **Server Actions** - Все действия проходят через сервер
3. **Revalidation** - Автоматическое обновление кэша

## Статистика

Дашборд показывает:

- **Ожидают проверки** - Задачи со статусом `pending`
- **На проверке** - Задачи со статусом `in_review`
- **Одобрено сегодня** - Задачи, одобренные за текущий день
- **Отклонено сегодня** - Задачи, отклоненные за текущий день

## Устранение неполадок

### Не вижу админ-панель

1. Проверьте роль пользователя:
```sql
SELECT email, role FROM profiles WHERE email = 'ваш@email.com';
```

2. Убедитесь, что миграция применена:
```sql
SELECT * FROM moderation_tasks LIMIT 1;
```

### Ошибка доступа к данным

1. Проверьте RLS политики:
```sql
SELECT * FROM pg_policies WHERE tablename = 'communities';
```

2. Убедитесь, что пользователь аутентифицирован:
```sql
SELECT auth.uid(); -- Должен вернуть UUID
```

### Задачи модерации не создаются

1. Проверьте триггер:
```sql
SELECT * FROM pg_trigger WHERE tgname = 'create_community_moderation_task_trigger';
```

2. Создайте задачу вручную:
```sql
INSERT INTO moderation_tasks (content_type, content_id, status)
VALUES ('community', 'community_uuid', 'pending');
```

## Дополнительные возможности

### Пакетные операции

Для массовой публикации/снятия с публикации можно использовать SQL:

```sql
-- Опубликовать все сообщества определенной категории
UPDATE communities 
SET is_published = true 
WHERE category_id = 'category_uuid';

-- Деактивировать всех экспертов с рейтингом ниже 3.0
UPDATE experts 
SET is_active = false 
WHERE rating < 3.0;
```

### Расширение функционала

Для добавления новых типов модерации:

1. Добавьте тип в `content_type` ENUM
2. Создайте триггер для автоматического создания задач
3. Добавьте функции управления в `lib/supabase/admin.ts`
4. Создайте UI компоненты

## Полезные SQL-запросы

```sql
-- Статистика модерации за последние 7 дней
SELECT 
  DATE(created_at) as date,
  status,
  COUNT(*) as count
FROM moderation_tasks
WHERE created_at >= NOW() - INTERVAL '7 days'
GROUP BY DATE(created_at), status
ORDER BY date DESC;

-- Топ-10 создателей контента
SELECT 
  p.email,
  p.full_name,
  COUNT(c.id) as communities_count
FROM profiles p
LEFT JOIN communities c ON c.owner_id = p.id
GROUP BY p.id
ORDER BY communities_count DESC
LIMIT 10;

-- Неактивные сообщества (без обновлений более 30 дней)
SELECT 
  name,
  slug,
  updated_at,
  is_published
FROM communities
WHERE updated_at < NOW() - INTERVAL '30 days'
ORDER BY updated_at DESC;
```

## Поддержка

При возникновении проблем:

1. Проверьте логи в Supabase Dashboard → Logs
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что все переменные окружения настроены
4. Обратитесь к документации Supabase: https://supabase.com/docs
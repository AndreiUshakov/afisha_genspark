meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  env:
    NODE_ENV: production
  script: |
    npm ci
    npm run build
    echo "Contents after build:"
    ls -laR .next

run:
  containerPort: 3000
  workDir: /app
  command: npm start

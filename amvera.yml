meta:
  environment: node
  toolchain:
    name: npm
    version: latest

build:
  env:
    NODE_ENV: production
  commands:
    - npm ci
    - npm run build

run:
  port: 3000
  persistStore: /app/.next
  command: npm run start

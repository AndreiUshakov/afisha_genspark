meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  additionalCommands: npm run build
  artifacts:
    "*": /.next
run:
  persistenceMount: /data
  containerPort: 3000
  command: npm start

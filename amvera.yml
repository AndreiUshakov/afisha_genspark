meta:
  environment: node
  toolchain:
    name: npm
    version: 22
build:
  artifacts:
    '*': /
  additionalCommands: npm run build
  command: npm start
run:
  persistenceMount: /data
  containerPort: 3000

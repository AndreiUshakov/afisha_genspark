meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  env:
    NODE_ENV: production
    NEXT_TELEMETRY_DISABLED: 1
  script: |
    npm ci --prefer-offline --no-audit
    npm run build

run:
  containerPort: 3000
  command: npm start

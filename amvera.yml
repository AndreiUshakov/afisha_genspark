meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  env:
    NODE_ENV: production
    NODE_OPTIONS: --max-old-space-size=4096
  script: |
    npm ci
    npm run build

run:
  containerPort: 3000
  command: npm start

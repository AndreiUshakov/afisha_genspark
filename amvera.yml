meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  env:
    NODE_ENV: production
  script: |
    npm install --legacy-peer-deps
    npm run build

run:
  containerPort: 3000
  command: npm start

meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  env:
    NODE_ENV: production
  script: |
    npm ci
    npm run build

run:
  containerPort: 3000
  command: node_modules/.bin/next start

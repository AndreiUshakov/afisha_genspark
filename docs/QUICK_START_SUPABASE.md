# 🚀 Быстрый старт: Интеграция Supabase

## Текущий статус
✅ Переменные окружения настроены  
✅ Supabase клиенты созданы ([`lib/supabase/client.ts`](../lib/supabase/client.ts), [`lib/supabase/server.ts`](../lib/supabase/server.ts))  
✅ SQL-схема готова ([`supabase/schema.sql`](../supabase/schema.sql))  
⏳ Таблицы нужно создать  
⏳ Storage buckets нужно создать  

---

## 📋 Пошаговая инструкция

### Шаг 1: Создание таблиц в Supabase

**Способ 1: Через Supabase Dashboard (Рекомендуется)**

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (левое меню)
4. Нажмите **New query**
5. Откройте файл [`supabase/schema.sql`](../supabase/schema.sql) в вашем редакторе
6. Скопируйте **ВСЁ** содержимое (682 строки)
7. Вставьте в SQL Editor
8. Нажмите **Run** (или Ctrl+Enter)
9. Дождитесь сообщения "Success. No rows returned"

**Что будет создано:**
- ✅ 10 таблиц (profiles, categories, communities, experts, events, posts, favorites, event_registrations, reviews, community_members)
- ✅ Все индексы для оптимизации запросов
- ✅ RLS политики для безопасности
- ✅ Триггеры для автоматических обновлений
- ✅ Функции для бизнес-логики

---

### Шаг 2: Создание Storage Buckets

1. В Supabase Dashboard перейдите в **Storage** (левое меню)
2. Создайте 5 buckets (нажимайте "New bucket" для каждого):

| Bucket | Public | Size Limit | MIME Types |
|--------|--------|------------|------------|
| `avatars` | ✅ Yes | 2 MB | image/jpeg, image/png, image/webp |
| `covers` | ✅ Yes | 5 MB | image/jpeg, image/png, image/webp |
| `events` | ✅ Yes | 5 MB | image/jpeg, image/png, image/webp |
| `communities` | ✅ Yes | 5 MB | image/jpeg, image/png, image/webp |
| `posts` | ✅ Yes | 5 MB | image/jpeg, image/png, image/webp |

---

### Шаг 3: Проверка подключения

Запустите тестовый скрипт:

```bash
npm run test:supabase
```

**Ожидаемый результат:**
```
🔍 Проверка подключения к Supabase...
✅ Подключение успешно!
✅ Таблица "profiles" доступна
✅ Таблица "categories" доступна
...
✨ Все проверки пройдены успешно!
```

**Если есть ошибки:**
- Проверьте `.env.local` (ключи должны быть корректными)
- Убедитесь, что SQL-скрипт выполнен полностью
- Проверьте, что все таблицы созданы в Table Editor

---

### Шаг 4: Миграция mock-данных (опционально)

После успешной проверки можно мигрировать тестовые данные:

```bash
# Мигрировать всё сразу
npm run migrate:all

# Или по отдельности
npm run migrate:categories
npm run migrate:events
npm run migrate:communities
npm run migrate:experts
npm run migrate:posts
```

**Примечание:** Скрипты миграции нужно будет создать отдельно.

---

## ✅ Проверка результатов

### В Supabase Dashboard:

1. **Table Editor** → должны быть видны все 10 таблиц
2. **Storage** → должны быть созданы 5 buckets
3. **Authentication** → Email provider должен быть включен

### В вашем приложении:

```bash
# Запустите dev-сервер
npm run dev
```

Откройте http://localhost:3000 - приложение должно работать с реальной БД.

---

## 🎯 Следующие шаги

После успешной настройки:

1. ✅ Создать первого пользователя через регистрацию
2. ✅ Протестировать создание события
3. ✅ Протестировать создание сообщества
4. ✅ Проверить загрузку изображений
5. ✅ Проверить работу избранного

---

## 📚 Полезные ссылки

- [Подробная инструкция](./SUPABASE_SETUP_GUIDE.md)
- [План интеграции](./SUPABASE_INTEGRATION_PLAN.md)
- [Архитектура](./SUPABASE_ARCHITECTURE.md)
- [Supabase Documentation](https://supabase.com/docs)

---

## ❓ Частые вопросы

**Q: Нужно ли создавать таблицы вручную или через API?**  
A: Рекомендуется через SQL Editor в Dashboard - это быстрее и надежнее. API используется для работы с данными, а не для создания схемы.

**Q: Что делать, если таблицы уже существуют?**  
A: SQL-скрипт использует `CREATE TABLE IF NOT EXISTS`, поэтому можно запускать повторно без ошибок.

**Q: Можно ли изменить схему после создания?**  
A: Да, используйте `ALTER TABLE` команды или создайте новую миграцию.

**Q: Как удалить все данные и начать заново?**  
A: В Table Editor выберите таблицу → три точки → Truncate table.

---

## 🆘 Помощь

Если возникли проблемы:

1. Проверьте [SUPABASE_SETUP_GUIDE.md](./SUPABASE_SETUP_GUIDE.md) - раздел "Возможные проблемы"
2. Запустите `npm run test:supabase` для диагностики
3. Проверьте логи в Supabase Dashboard → Logs
4. Убедитесь, что RLS политики не блокируют доступ

---

**Готово! 🎉**  
После выполнения всех шагов ваше приложение будет работать с реальной базой данных Supabase.
# Реализация проверки подтверждения email

## Обзор изменений

Реализована система, позволяющая пользователям входить в систему с неподтвержденным email, но ограничивающая создание сообществ и профилей экспертов до подтверждения email.

## Внесенные изменения

### 1. Аутентификация ([`app/auth/login/actions.ts`](../app/auth/login/actions.ts))

**Изменено:**
- Убрана проверка подтверждения email при входе
- Пользователи могут входить с неподтвержденным email
- Уведомление о необходимости подтверждения отображается в личном кабинете

### 2. Личный кабинет ([`app/dashboard/page.tsx`](../app/dashboard/page.tsx))

**Добавлено:**
- Получение реальных данных пользователя из Supabase
- Баннер с уведомлением о неподтвержденном email
- Отображение реальной статистики (избранное, подписки, события)
- Компоненты пустых состояний для отсутствующих данных
- Блокировка кнопок создания сообщества/эксперта для неподтвержденных пользователей

**Функционал:**
```typescript
// Проверка статуса подтверждения email
const isEmailVerified = data.user.email_confirmed_at !== null

// Условная блокировка действий
{!isEmailVerified && <EmailVerificationBanner email={data.user.email} />}
```

### 3. Создание сообщества ([`app/dashboard/create-community/page.tsx`](../app/dashboard/create-community/page.tsx))

**Добавлено:**
- Проверка подтверждения email при загрузке страницы
- Предупреждающий баннер для неподтвержденных пользователей
- Блокировка кнопки создания сообщества
- Информативное сообщение о необходимости подтверждения

**Логика проверки:**
```typescript
useEffect(() => {
  async function checkEmailVerification() {
    const supabase = createClient();
    const { data: { user } } = await supabase.auth.getUser();
    setIsEmailVerified(user.email_confirmed_at !== null);
  }
  checkEmailVerification();
}, []);
```

### 4. Профиль эксперта ([`app/dashboard/expert/page.tsx`](../app/dashboard/expert/page.tsx))

**Добавлено:**
- Проверка существования профиля эксперта
- Проверка подтверждения email
- Пустое состояние с инструкциями для неподтвержденных пользователей
- Блокировка создания профиля до подтверждения email

### 5. Новые компоненты

#### [`components/dashboard/EmailVerificationBanner.tsx`](../components/dashboard/EmailVerificationBanner.tsx)
Баннер с уведомлением о необходимости подтверждения email:
- Отображает email пользователя
- Кнопка повторной отправки письма (опционально)
- Возможность скрыть баннер
- Адаптивный дизайн с поддержкой темной темы

#### [`components/dashboard/EmptyState.tsx`](../components/dashboard/EmptyState.tsx)
Универсальный компонент для пустых состояний:
- Настраиваемая иконка
- Заголовок и описание
- Опциональная кнопка действия
- Используется для отображения отсутствующих данных

## Пользовательский опыт

### Для неподтвержденных пользователей:

1. **Вход в систему** ✅
   - Могут войти с неподтвержденным email
   - Видят все разделы личного кабинета

2. **Личный кабинет** ⚠️
   - Видят желтый баннер с просьбой подтвердить email
   - Могут просматривать события и добавлять в избранное
   - Кнопки создания сообщества/эксперта заблокированы

3. **Создание сообщества** 🔒
   - Видят предупреждающий баннер
   - Кнопка создания заблокирована
   - Четкое объяснение требований

4. **Профиль эксперта** 🔒
   - Видят пустое состояние с инструкциями
   - Кнопка создания профиля недоступна
   - Объяснение необходимости подтверждения

### Для подтвержденных пользователей:

1. **Полный доступ** ✅
   - Все функции доступны
   - Нет ограничений
   - Баннеры не отображаются

## Технические детали

### Проверка статуса email

```typescript
// Server-side (Next.js Server Components)
const { data: { user } } = await supabase.auth.getUser()
const isEmailVerified = user.email_confirmed_at !== null

// Client-side (React Components)
const supabase = createClient()
const { data: { user } } = await supabase.auth.getUser()
const isEmailVerified = user.email_confirmed_at !== null
```

### Условная блокировка UI

```typescript
<button
  disabled={!isEmailVerified}
  className={isEmailVerified ? 'enabled-styles' : 'disabled-styles'}
  onClick={(e) => {
    if (!isEmailVerified) {
      e.preventDefault()
      alert('Подтвердите email')
    }
  }}
>
  {isEmailVerified ? 'Создать' : '🔒 Подтвердите email'}
</button>
```

## Безопасность

- Проверка выполняется как на клиенте, так и на сервере
- RLS политики Supabase обеспечивают дополнительную защиту
- Пользователи не могут обойти ограничения через API

## Будущие улучшения

1. Добавить функцию повторной отправки письма подтверждения
2. Реализовать таймер для повторной отправки
3. Добавить уведомления об успешном подтверждении
4. Создать страницу с инструкциями по подтверждению email

## Связанные файлы

- [`app/auth/login/actions.ts`](../app/auth/login/actions.ts) - Логика входа
- [`app/dashboard/page.tsx`](../app/dashboard/page.tsx) - Главная страница дашборда
- [`app/dashboard/create-community/page.tsx`](../app/dashboard/create-community/page.tsx) - Создание сообщества
- [`app/dashboard/expert/page.tsx`](../app/dashboard/expert/page.tsx) - Дашборд эксперта
- [`components/dashboard/EmailVerificationBanner.tsx`](../components/dashboard/EmailVerificationBanner.tsx) - Баннер уведомления
- [`components/dashboard/EmptyState.tsx`](../components/dashboard/EmptyState.tsx) - Пустые состояния
- [`supabase/schema.sql`](../supabase/schema.sql) - Схема БД
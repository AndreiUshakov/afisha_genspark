# Реализация создания сообщества

## ✅ Что реализовано

### 1. Server Actions для работы с сообществами
**Файл:** [`app/dashboard/create-community/actions.ts`](../app/dashboard/create-community/actions.ts)

Функции:
- `createCommunity()` - создание нового сообщества
- `uploadCommunityImage()` - загрузка изображений (аватар/обложка)
- `updateCommunity()` - обновление данных сообщества
- `publishCommunity()` - публикация сообщества

### 2. Обновленная страница создания
**Файл:** [`app/dashboard/create-community/page.tsx`](../app/dashboard/create-community/page.tsx)

Изменения:
- ✅ Интеграция с реальной базой данных Supabase
- ✅ Валидация всех обязательных полей
- ✅ Проверка подтверждения email пользователя
- ✅ Проверка уникальности slug
- ✅ Индикатор загрузки при создании
- ✅ Обработка ошибок с понятными сообщениями
- ✅ Перенаправление на страницу управления после создания

### 3. Структура данных
**Таблица:** `communities` (уже существует в БД)

Поля:
- `id` - UUID сообщества
- `owner_id` - ID владельца (ссылка на profiles)
- `category_id` - ID категории
- `name` - название сообщества
- `slug` - уникальный URL-адрес
- `description` - краткое описание
- `avatar_url` - URL аватара
- `cover_url` - URL обложки
- `location` - адрес/район
- `social_links` - JSONB с социальными сетями
- `target_audience` - массив целевой аудитории
- `wishes` - массив желаний/целей
- `age_category` - возрастная категория
- `page_content` - JSONB с CraftJS контентом
- `photo_albums` - JSONB с альбомами
- `is_published` - флаг публикации
- `is_verified` - флаг верификации
- `created_at` - дата создания
- `updated_at` - дата обновления

## 🔐 Безопасность

### RLS Политики (уже настроены)
1. ✅ Опубликованные сообщества видны всем
2. ✅ Владельцы могут создавать сообщества
3. ✅ Владельцы могут обновлять свои сообщества
4. ✅ Владельцы могут удалять свои сообщества

### Валидация
- ✅ Проверка авторизации пользователя
- ✅ Проверка подтверждения email
- ✅ Проверка уникальности slug
- ✅ Валидация всех обязательных полей
- ✅ Проверка прав владельца при обновлении/удалении

## 📦 Необходимые настройки на хостинге

### 1. Создать Storage Bucket

Выполните в Supabase SQL Editor:

```sql
-- Создать bucket для изображений сообществ
INSERT INTO storage.buckets (id, name, public)
VALUES ('community-images', 'community-images', true);

-- Политика: Публичный доступ на чтение
CREATE POLICY "Публичный доступ на чтение" ON storage.objects
  FOR SELECT
  USING (bucket_id = 'community-images');

-- Политика: Авторизованные пользователи могут загружать
CREATE POLICY "Авторизованные пользователи могут загружать" ON storage.objects
  FOR INSERT
  WITH CHECK (
    bucket_id = 'community-images' 
    AND auth.uid() IS NOT NULL
  );

-- Политика: Пользователи могут обновлять свои файлы
CREATE POLICY "Пользователи могут обновлять свои файлы" ON storage.objects
  FOR UPDATE
  USING (
    bucket_id = 'community-images' 
    AND auth.uid() = owner
  );

-- Политика: Пользователи могут удалять свои файлы
CREATE POLICY "Пользователи могут удалять свои файлы" ON storage.objects
  FOR DELETE
  USING (
    bucket_id = 'community-images' 
    AND auth.uid() = owner
  );
```

### 2. Убедиться в наличии категорий

Проверьте, что в таблице `categories` есть записи, соответствующие:
```typescript
FILTER_OPTIONS.CATEGORIES = [
  { id: 'cat-culture', name: 'Культура и искусство' },
  { id: 'cat-sport', name: 'Спорт и здоровье' },
  { id: 'cat-education', name: 'Образование' },
  { id: 'cat-hobby', name: 'Хобби и увлечения' },
  { id: 'cat-business', name: 'Бизнес и карьера' },
  { id: 'cat-social', name: 'Социальные проекты' },
  { id: 'cat-tech', name: 'Технологии' },
  { id: 'cat-family', name: 'Семья и дети' },
  { id: 'cat-ecology', name: 'Экология' },
  { id: 'cat-other', name: 'Другое' }
]
```

Если категорий нет, выполните:

```sql
INSERT INTO categories (id, name, slug, color) VALUES
('cat-culture', 'Культура и искусство', 'culture', '#8B5CF6'),
('cat-sport', 'Спорт и здоровье', 'sport', '#10B981'),
('cat-education', 'Образование', 'education', '#3B82F6'),
('cat-hobby', 'Хобби и увлечения', 'hobby', '#F59E0B'),
('cat-business', 'Бизнес и карьера', 'business', '#6366F1'),
('cat-social', 'Социальные проекты', 'social', '#EC4899'),
('cat-tech', 'Технологии', 'tech', '#06B6D4'),
('cat-family', 'Семья и дети', 'family', '#F97316'),
('cat-ecology', 'Экология', 'ecology', '#84CC16'),
('cat-other', 'Другое', 'other', '#6B7280');
```

## 🧪 Тестирование

### Для проверки функциональности:

1. **Войдите** под зарегистрированным пользователем
2. **Подтвердите email** (проверьте почту)
3. Перейдите на `/dashboard/create-community`
4. **Заполните форму:**
   - Название сообщества (мин. 3 символа)
   - Категория (выберите из списка)
   - Краткое описание (20-200 символов)
   - Email для связи (валидный email)
   - Опционально: локация, телефон, соц. сети
5. **Выберите фильтры:**
   - Для кого (целевая аудитория)
   - Я хочу (желания/цели)
   - Возрастные категории
6. Нажмите **"Создать сообщество"**

### Ожидаемый результат:
- ✅ Сообщество создано в БД с `is_published = false`
- ✅ Перенаправление на `/dashboard/community`
- ✅ Сообщество доступно для редактирования владельцу
- ✅ Сообщество НЕ видно в публичном списке до публикации

## 🚀 Следующие шаги

### Для полной функциональности:

1. **Страница управления сообществом**
   - Просмотр созданного сообщества
   - Редактирование данных
   - Загрузка изображений (аватар/обложка)
   - Публикация сообщества

2. **Страница просмотра сообщества**
   - Публичная страница `/communities/[slug]`
   - Отображение всех данных
   - Кнопки подписки/вступления

3. **Шаг 2: Визуальный редактор**
   - Загрузка обложки и логотипа
   - CraftJS page builder для кастомизации
   - Сохранение page_content

## 📝 Примечания

- По умолчанию сообщества создаются как черновики (`is_published = false`)
- Требуется подтверждение email для создания сообщества
- Slug генерируется автоматически из названия с транслитерацией
- Все поля формы валидируются на клиенте и сервере
- Изображения хранятся в Supabase Storage в папке `communities/{slug}/`

## 🔗 Связанные файлы

- [`types/community.ts`](../types/community.ts) - типы и константы
- [`utils/validation.ts`](../utils/validation.ts) - функции валидации
- [`utils/slug.ts`](../utils/slug.ts) - генерация slug
- [`supabase/schema.sql`](../supabase/schema.sql) - схема БД
- [`docs/COMMUNITY_STORAGE_SETUP.md`](./COMMUNITY_STORAGE_SETUP.md) - настройка Storage
# Реализация форм создания и редактирования мероприятий

## Обзор изменений

Реализованы формы создания и редактирования основных настроек мероприятий с поддержкой новых полей фильтрации и автоматической загрузкой настроек из сообщества.

## 1. Миграция базы данных

### Файл: `supabase/migrations/add_event_filter_fields.sql`

Добавлены новые поля в таблицу `events`:
- `event_type` (TEXT) - Тип мероприятия из категории сообщества
- `target_audience` (TEXT[]) - Массив целевой аудитории ("Для кого")
- `wishes` (TEXT[]) - Массив целей/желаний ("Я хочу")
- `age_categories` (TEXT[]) - Массив возрастных категорий

Созданы индексы для эффективной фильтрации:
- `idx_events_event_type` - B-tree индекс для типа мероприятия
- `idx_events_target_audience` - GIN индекс для массива аудитории
- `idx_events_wishes` - GIN индекс для массива желаний
- `idx_events_age_categories` - GIN индекс для возрастных категорий

## 2. Обновление типов и интерфейсов

### Файл: `app/dashboard/community/[slug]/events/create/actions.ts`

Обновлен интерфейс `CreateEventBasicData`:
```typescript
export interface CreateEventBasicData {
  // ... существующие поля
  event_type: string | null
  target_audience: string[]
  wishes: string[]
  age_categories: string[]
}
```

Функция `createEventDraft` теперь сохраняет новые поля в базу данных.

## 3. Форма создания мероприятия

### Файл: `app/dashboard/community/[slug]/events/create/basic/page.tsx`

**Изменения:**
- Загрузка сообщества с категорией через join: `select('*, categories(id, name, slug)')`
- Передача полной информации о сообществе в форму

### Файл: `app/dashboard/community/[slug]/events/create/basic/components/BasicEventForm.tsx`

**Ключевые изменения:**

1. **Автозаполнение из сообщества:**
   - Категория мероприятия автоматически берется из `community.category_id`
   - Тип мероприятия заполняется названием категории сообщества
   - Поля фильтрации (`target_audience`, `wishes`, `age_categories`) инициализируются значениями из сообщества

2. **Упрощенный формат мероприятия:**
   - Удален вариант "Гибрид" (hybrid)
   - Оставлены только "Офлайн" (physical) и "Онлайн" (online)
   - Изменена сетка с 3 колонок на 2 колонки

3. **Новый раздел "Фильтры и аудитория":**
   - Добавлен компонент `FilterSelector` для каждого типа фильтра
   - Подсказка о возможности изменения настроек по умолчанию
   - Использование `FILTER_OPTIONS` из `types/community`

4. **Структура полей:**
   ```tsx
   - Для кого (target_audience)
   - Я хочу (wishes)
   - Возраст (age_categories)
   ```

## 4. Форма редактирования мероприятия

### Новые файлы:

#### `app/dashboard/community/[slug]/events/[id]/edit/basic/page.tsx`
- Загрузка мероприятия и сообщества
- Проверка прав доступа
- Рендеринг формы редактирования

#### `app/dashboard/community/[slug]/events/[id]/edit/basic/components/BasicEventEditForm.tsx`
- Полная копия функциональности формы создания
- Инициализация полей данными существующего мероприятия
- Преобразование дат в формат `datetime-local` для input полей
- Использование `updateEventBasic` вместо `createEventDraft`
- Автообновление страницы после успешного сохранения

## 5. Структура данных

### Поля формы

```typescript
interface FormData {
  // Основная информация
  title: string
  description: string
  category_id: string
  
  // Дата и время
  start_date: string
  end_date: string | null
  is_recurring: boolean
  recurrence_pattern: string | null
  
  // Место проведения
  location_type: 'physical' | 'online'
  venue_name: string | null
  venue_address: string | null
  online_link: string | null
  
  // Стоимость
  is_free: boolean
  price_min: number | null
  price_max: number | null
  currency: string
  ticket_link: string | null
  capacity: number | null
  
  // Фильтры (НОВОЕ)
  event_type: string | null
  target_audience: string[]
  wishes: string[]
  age_categories: string[]
  
  // Дополнительно
  age_restriction: string | null
  tags: string[]
}
```

## 6. Валидация

Обязательные поля:
- Название (`title`)
- Описание (`description`)
- Категория (`category_id`)
- Дата начала (`start_date`)
- Место проведения для офлайн (`venue_name`)
- Ссылка для онлайн (`online_link`)
- Минимальная цена для платных (`price_min`)

## 7. Пользовательский опыт

### Форма создания:
1. При создании мероприятия категория автоматически выбирается из сообщества
2. Фильтры предзаполнены значениями по умолчанию из настроек сообщества
3. Пользователь может изменить любые предзаполненные значения
4. После создания редирект на список мероприятий

### Форма редактирования:
1. Все поля заполнены текущими значениями мероприятия
2. Изменения сохраняются при нажатии "Сохранить"
3. Отображается уведомление об успехе/ошибке
4. Страница автоматически обновляется после успешного сохранения

## 8. Маршруты

### Создание:
- `/dashboard/community/[slug]/events/create/basic` - форма создания

### Редактирование:
- `/dashboard/community/[slug]/events/[id]/edit` - выбор раздела
- `/dashboard/community/[slug]/events/[id]/edit/basic` - редактирование основных настроек

## 9. Использованные компоненты

- `FilterSelector` - компонент множественного выбора с поиском
- `FILTER_OPTIONS` - константы с вариантами фильтров из `types/community`

## 10. База данных

### Запуск миграции:

```bash
# Локально через Supabase CLI
supabase db reset

# Или применить миграцию вручную
supabase migration up
```

### Проверка изменений:

```sql
-- Просмотр структуры таблицы
\d events

-- Проверка индексов
SELECT indexname, indexdef 
FROM pg_indexes 
WHERE tablename = 'events' 
AND indexname LIKE 'idx_events_%';
```

## 11. Следующие шаги

1. ✅ Создать миграцию базы данных
2. ✅ Обновить типы и интерфейсы
3. ✅ Модифицировать форму создания
4. ✅ Реализовать форму редактирования
5. ✅ Упростить формат мероприятия (только Офлайн/Онлайн)
6. 🔄 Протестировать создание мероприятия
7. 🔄 Протестировать редактирование мероприятия
8. 🔄 Проверить фильтрацию в каталоге мероприятий

## 12. Технические детали

### Особенности реализации:

1. **Преобразование дат:**
   ```typescript
   start_date: event.start_date.slice(0, 16) // ISO -> datetime-local
   ```

2. **Обработка массивов:**
   ```typescript
   target_audience: event.target_audience || []
   ```

3. **Условный рендеринг полей:**
   ```typescript
   {formData.location_type === 'physical' && (
     // Поля для офлайн
   )}
   ```

4. **Использование FilterSelector:**
   ```typescript
   <FilterSelector
     label="Для кого"
     options={FILTER_OPTIONS.TARGET_AUDIENCE}
     selected={formData.target_audience}
     onChange={(selected) => setFormData({ 
       ...formData, 
       target_audience: selected 
     })}
   />
   ```

## 13. Примечания

- Все изменения обратно совместимы
- Старые мероприятия без новых полей будут иметь пустые массивы
- Индексы оптимизируют фильтрацию в каталоге
- Категория мероприятия может отличаться от категории сообщества
- Тип мероприятия (`event_type`) используется для группировки в фильтрах
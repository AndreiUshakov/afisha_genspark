# Руководство по блоку "Карусель изображений"

## Обзор

Добавлен новый тип блока контента для страниц сообществ - **"Карусель изображений"**. Этот блок позволяет создавать адаптивные слайдеры с множественными изображениями.

## Изменения в базе данных

### Миграция
Файл: `supabase/migrations/add_carousel_block_type.sql`

Добавлен новый тип блока `'carousel'` в constraint таблицы `community_content_blocks`:
- `'heading'` - заголовок
- `'text'` - текстовый блок
- `'image'` - одиночное изображение
- **`'carousel'`** - карусель изображений (НОВОЕ)

### Применение миграции
```bash
cd supabase
npx supabase db push
```

## Структура данных

### TypeScript типы
Файл: `lib/types/content-blocks.ts`

```typescript
export interface CarouselImage {
  url: string           // URL изображения
  alt: string          // Альтернативный текст
  caption?: string     // Подпись (опционально)
}

export interface CarouselBlockContent {
  images: CarouselImage[]              // Массив изображений
  slidesPerView?: number               // Слайдов на десктопе (по умолчанию: 4)
  slidesPerViewMobile?: number         // Слайдов на мобильных (по умолчанию: 1)
}
```

### Пример JSON в БД
```json
{
  "images": [
    {
      "url": "https://example.com/image1.jpg",
      "alt": "Описание изображения 1",
      "caption": "Подпись к изображению 1"
    },
    {
      "url": "https://example.com/image2.jpg",
      "alt": "Описание изображения 2"
    }
  ],
  "slidesPerView": 4,
  "slidesPerViewMobile": 1
}
```

## Компоненты

### 1. CarouselBlockEditor
**Путь:** `app/dashboard/community/[slug]/settings/content/components/CarouselBlockEditor.tsx`

Редактор карусели в админ-панели с возможностями:
- ✅ Загрузка изображений из медиагалереи
- ✅ Загрузка новых изображений
- ✅ Настройка количества слайдов для десктопа (1-6)
- ✅ Настройка количества слайдов для мобильных (1-3)
- ✅ Редактирование alt-текста для каждого изображения
- ✅ Добавление подписей к изображениям
- ✅ Изменение порядка изображений (стрелки вверх/вниз)
- ✅ Удаление отдельных изображений

### 2. CarouselBlock
**Путь:** `components/content-blocks/CarouselBlock.tsx`

Компонент отображения карусели на публичной странице:
- Использует библиотеку Swiper
- Адаптивная настройка слайдов
- Автоматическая прокрутка (3 сек)
- Навигация (стрелки)
- Пагинация (точки)
- Плавные переходы

### 3. ContentBuilder
**Путь:** `app/dashboard/community/[slug]/settings/content/components/ContentBuilder.tsx`

Обновлен для поддержки нового типа блока:
- Добавлена кнопка "Карусель изображений" с оранжевой темой
- Поддержка создания блока с пустым массивом изображений
- Рендеринг `CarouselBlockEditor` для редактирования

### 4. ContentBlocksPreview
**Путь:** `components/content-blocks/ContentBlocksPreview.tsx`

Обновлен для отображения карусели на публичной странице сообщества

## Использование

### Для администраторов сообщества

1. Перейдите в настройки сообщества: `/dashboard/community/[slug]/settings/content`
2. Нажмите кнопку **"+ Добавить блок"**
3. Выберите **"Карусель изображений"** (оранжевая иконка)
4. Настройте параметры:
   - **Слайдов на десктопе**: 1-6 (по умолчанию 4)
   - **Слайдов на мобильных**: 1-3 (по умолчанию 1)
5. Добавьте изображения:
   - **Кнопка "Загрузить изображение"**: загрузить новое фото
   - **Кнопка "Из галереи"**: выбрать из медиагалереи сообщества
6. Для каждого изображения:
   - Укажите **Alt-текст** (обязательно для доступности)
   - Добавьте **подпись** (опционально)
   - Измените порядок кнопками ⬆️ ⬇️
   - Удалите ненужные изображения кнопкой 🗑️
7. Нажмите **"Сохранить"**

### Адаптивные настройки

Карусель автоматически адаптируется под разные размеры экрана:

| Размер экрана | Слайдов по умолчанию |
|---------------|----------------------|
| < 640px       | 1 (мобильная настройка) |
| 640px - 768px | min(2, desktop настройка) |
| 768px - 1024px| min(3, desktop настройка) |
| > 1024px      | desktop настройка (по умолчанию 4) |

## Технические детали

### Используемые технологии
- **Swiper 12.0.3**: библиотека для карусели (уже установлена)
- **Next.js Image**: оптимизация изображений
- **Tailwind CSS**: стилизация

### Swiper модули
- `Navigation`: навигация стрелками
- `Pagination`: точки внизу
- `Autoplay`: автоматическая прокрутка

### Стилизация
- Кастомные стили для кнопок навигации (черные с прозрачностью)
- Оранжевый цвет для активной точки пагинации
- Поддержка темной темы
- Hover эффекты для изображений (scale: 1.1)

## Примеры использования

### Пример 1: Фотогалерея мероприятия
```
Слайдов на десктопе: 3
Слайдов на мобильных: 1
Изображений: 12 фото с мероприятия
```

### Пример 2: Витрина продуктов/услуг
```
Слайдов на десктопе: 4
Слайдов на мобильных: 1
Изображений: 8 товаров с описаниями в подписях
```

### Пример 3: Портфолио работ
```
Слайдов на десктопе: 2
Слайдов на мобильных: 1
Изображений: 6 проектов с детальными подписями
```

## Ограничения

- Минимум 1 изображение для сохранения блока
- Максимум изображений не ограничен (практический лимит ~50 для производительности)
- Размер файла: максимум 5 МБ на изображение
- Форматы: JPEG, PNG, WebP

## Troubleshooting

### Карусель не отображается
1. Проверьте, что миграция БД применена
2. Убедитесь, что в блоке есть хотя бы одно изображение
3. Проверьте консоль браузера на ошибки

### Изображения не загружаются
1. Проверьте права доступа к bucket `community-media` в Supabase Storage
2. Убедитесь, что изображения не превышают 5 МБ
3. Проверьте CORS настройки в Supabase

### Стили Swiper не применяются
1. Убедитесь, что импорты CSS присутствуют в `CarouselBlock.tsx`
2. Проверьте, что Swiper установлен: `npm list swiper`

## Файлы изменений

### Созданные файлы
- ✅ `supabase/migrations/add_carousel_block_type.sql`
- ✅ `app/dashboard/community/[slug]/settings/content/components/CarouselBlockEditor.tsx`
- ✅ `components/content-blocks/CarouselBlock.tsx`
- ✅ `CAROUSEL_BLOCK_GUIDE.md`

### Измененные файлы
- ✅ `lib/types/content-blocks.ts`
- ✅ `app/dashboard/community/[slug]/settings/content/components/ContentBuilder.tsx`
- ✅ `components/content-blocks/ContentBlocksPreview.tsx`

## Следующие шаги

После применения миграции БД:
1. Перезапустите dev сервер: `npm run dev`
2. Откройте страницу редактирования контента сообщества
3. Создайте тестовый блок "Карусель изображений"
4. Добавьте несколько изображений
5. Проверьте отображение на публичной странице сообщества
6. Протестируйте адаптивность на разных устройствах

## Поддержка

Если возникли вопросы или проблемы, проверьте:
- Логи сервера Next.js
- Консоль браузера
- Network tab для запросов к Supabase
- Supabase Dashboard для проверки данных в БД
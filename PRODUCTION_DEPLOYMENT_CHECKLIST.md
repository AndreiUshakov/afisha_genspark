# Чеклист для деплоя на Production (Amvera)

## ✅ Что уже сделано в коде

- [x] Добавлен параметр `emailRedirectTo` в функцию регистрации
- [x] Улучшена обработка ошибок в callback route
- [x] Добавлена обработка ошибок из URL на странице логина
- [x] Создана документация по настройке email подтверждения

## 🔧 Что нужно сделать на Amvera

### 1. Обновить переменные окружения

В панели управления Amvera обновите:

```
NEXT_PUBLIC_SITE_URL=https://afisa-ushakovandrei.amvera.io
```

**Важно:** Удалите старое значение `http://localhost:3000`

### 2. Проверить конфигурацию Supabase Auth

Убедитесь, что в контейнере `supabase-auth` установлены:

```yaml
environment:
  SITE_URL: "https://afisa-ushakovandrei.amvera.io"
  ADDITIONAL_REDIRECT_URLS: "https://afisa-ushakovandrei.amvera.io/auth/callback"
  
  # SMTP настройки (уже должны быть настроены)
  GOTRUE_SMTP_HOST: "ваш-smtp-сервер"
  GOTRUE_SMTP_PORT: "587"
  GOTRUE_SMTP_USER: "info@garant-city.ru"
  GOTRUE_SMTP_PASS: "ваш-пароль"
  GOTRUE_SMTP_ADMIN_EMAIL: "info@garant-city.ru"
  
  # Для production отключите автоподтверждение
  GOTRUE_MAILER_AUTOCONFIRM: "false"
```

### 3. Перезапустить приложение

После обновления переменных окружения:
1. Перезапустите контейнер Next.js приложения
2. Перезапустите контейнер supabase-auth (если меняли его конфиг)

## 🧪 Тестирование

### Шаг 1: Регистрация нового пользователя

1. Откройте https://afisa-ushakovandrei.amvera.io/auth/register
2. Зарегистрируйте нового пользователя с реальным email
3. Проверьте, что пользователь создался в Supabase

### Шаг 2: Проверка email

1. Откройте почтовый ящик
2. Найдите письмо с подтверждением
3. **Проверьте ссылку** - она должна вести на:
   ```
   https://afisa-ushakovandrei.amvera.io/auth/callback?code=...
   ```
   А НЕ на `https://supabase.sober-automation.ru`

### Шаг 3: Подтверждение email

1. Кликните по ссылке в письме
2. Должен произойти редирект на `/dashboard`
3. Проверьте, что пользователь авторизован

### Шаг 4: Проверка сессии

1. Обновите страницу
2. Сессия должна сохраниться
3. Пользователь должен оставаться авторизованным

## 🐛 Возможные проблемы

### Проблема: Ссылка все еще ведет на supabase.sober-automation.ru

**Решение:**
1. Проверьте `SITE_URL` в конфигурации supabase-auth
2. Перезапустите контейнер supabase-auth
3. Зарегистрируйте нового пользователя (старые письма будут со старой ссылкой)

### Проблема: "Invalid redirect URL"

**Решение:**
1. Добавьте URL в `ADDITIONAL_REDIRECT_URLS`
2. Формат: `https://afisa-ushakovandrei.amvera.io/auth/callback`
3. Перезапустите supabase-auth

### Проблема: Email не приходит

**Решение:**
1. Проверьте SMTP настройки в supabase-auth
2. Проверьте логи контейнера supabase-auth
3. Убедитесь, что `GOTRUE_SMTP_*` переменные установлены корректно

### Проблема: Ошибка при переходе по ссылке

**Решение:**
1. Проверьте логи Next.js приложения
2. Ошибка будет в консоли в [`app/auth/callback/route.ts`](app/auth/callback/route.ts:15)
3. Проверьте, что `NEXT_PUBLIC_SITE_URL` установлен правильно

## 📝 Логи для отладки

### Где смотреть логи:

1. **Next.js приложение** - логи Amvera контейнера
2. **Supabase Auth** - логи контейнера supabase-auth
3. **Email отправка** - логи SMTP в supabase-auth

### Что искать в логах:

```
# Успешная регистрация
"User created successfully"

# Отправка email
"Sending confirmation email to..."

# Успешное подтверждение
"Email confirmed for user..."

# Ошибки
"Error exchanging code for session"
"Invalid redirect URL"
"SMTP connection failed"
```

## 📚 Дополнительная документация

- [EMAIL_CONFIRMATION_SETUP.md](docs/EMAIL_CONFIRMATION_SETUP.md) - подробная инструкция
- [SUPABASE_SETUP_GUIDE.md](docs/SUPABASE_SETUP_GUIDE.md) - настройка Supabase
- [Supabase Auth Docs](https://supabase.com/docs/guides/auth)

## ✨ После успешного деплоя

- [ ] Протестировать регистрацию с разных email провайдеров (Gmail, Yandex, Mail.ru)
- [ ] Проверить работу на мобильных устройствах
- [ ] Убедиться, что сессия сохраняется после перезагрузки страницы
- [ ] Протестировать восстановление пароля (если реализовано)

---

**Готово!** 🎉 Регистрация пользователей должна работать корректно.